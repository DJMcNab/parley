[package]
name = "parley_glyph_positioning_cases"
description = "Shared cases and golden output for glyph-positioning tests"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
rand = { workspace = true }
rand_chacha = { workspace = true }
read-fonts = { workspace = true, features = ["std"] }

[lints]
workspace = true
